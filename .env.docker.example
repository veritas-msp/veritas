# Copy to .env in this directory before: docker compose up -d --build
# Generate JWT_SECRET and ENCRYPTION_KEY (64 random characters each).

POSTGRES_PASSWORD=changeme
JWT_SECRET=
ENCRYPTION_KEY=
VERITAS_EDITION=community
